"""Deep enrichment batch 022: 12 papers — remaining kernel-relevant EuroSys, NSDI, SOSP, SYSTOR.
"""
import json, sqlite3, uuid
DB_PATH = "data/master.db"

ALL_PAPERS = [
    {"ev_id": "ev-conf-88c7b0ab", "brief": {"key_ideas": ["Implements 35 representative network functions with eBPF but encounters inherent problems and limitations", "Identifies and addresses the challenges of building complex NFs in eBPF's constrained programming model", "eNetSTL provides an in-kernel library of reusable eBPF components for high-performance NFs", "Fills the gap between eBPF's promise for networking and the practical difficulty of implementing complex NFs"], "relevance": "Directly extends the eBPF networking ecosystem. The 35 NF implementations reveal which kernel eBPF constraints are blocking real networking use cases. The in-kernel library approach (reusable eBPF components) is how the kernel's eBPF ecosystem should evolve — from individual programs to composable libraries. Published at EuroSys.", "methodology": "EuroSys. 35 eBPF network function implementations. Constraint analysis. In-kernel library design."}, "concepts": ["eBPF (Extended Berkeley Packet Filter)", "XDP (eXpress Data Path)"], "claims": [{"kind": "Observation", "id_prefix": "obs", "name": "eBPF constraints block complex network function implementation", "description": "Implementing 35 representative network functions in eBPF reveals inherent problems: the constrained programming model blocks complex NF patterns. An in-kernel library of reusable components addresses this gap."}]},

    {"ev_id": "ev-conf-9e4f4e8c", "brief": {"key_ideas": ["eBPF-powered cross-layer storage profiling tool for ZNS SSDs", "Profiles ZNS SSD behavior across filesystem, block layer, and device layers using eBPF instrumentation", "Leverages ZNS's open interface to correlate host I/O patterns with device-internal behavior", "First comprehensive cross-layer profiling tool for zoned storage"], "relevance": "Combines eBPF kernel tracing with ZNS SSD profiling — uses the kernel's eBPF infrastructure to instrument the storage stack. Cross-layer means it traces through VFS → filesystem → block layer → NVMe driver → device, correlating events across all kernel layers. Relevant to the kernel's blk-zoned, NVMe ZNS support, and eBPF tracepoints in the storage path. Published at EuroSys.", "methodology": "EuroSys. eBPF-based ZNS SSD profiling. Cross-layer storage stack instrumentation."}, "concepts": ["eBPF (Extended Berkeley Packet Filter)", "NVMe Driver Subsystem", "Block Device Layer"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "eBPF cross-layer profiling for ZNS SSD storage stack", "description": "zns-tools uses eBPF to profile ZNS SSD behavior across filesystem, block layer, and device layers. First comprehensive cross-layer profiling correlating host I/O patterns with ZNS device internals."}]},

    {"ev_id": "ev-conf-456e4dc7", "brief": {"key_ideas": ["Rethinks write-ahead logs for ZNS SSDs using zone append operations", "ZNS SSDs require sequential writes — WAL is naturally sequential but current implementations don't exploit zone append", "Zone append eliminates host-side write ordering overhead for WAL, improving throughput", "Evaluated with LSM-tree KV stores which heavily depend on WAL performance"], "relevance": "Directly relevant to the kernel's ZNS support. Zone append is a kernel block layer operation (blk_zone_append) — ZWAL shows how to use it for database WAL. The kernel's blk-zoned subsystem provides the zone append interface that ZWAL depends on. Relevant to the kernel's NVMe ZNS driver and F2FS/btrfs ZNS support. Published at EuroSys.", "methodology": "EuroSys. Zone append for write-ahead logging. ZNS SSD optimization. LSM-tree KV store evaluation."}, "concepts": ["NVMe Driver Subsystem", "Block Device Layer"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Zone append-based write-ahead logging for ZNS SSDs", "description": "ZWAL exploits ZNS zone append operations for write-ahead logs, eliminating host-side write ordering overhead. Natural fit: WAL is sequential and ZNS requires sequential writes."}]},

    {"ev_id": "ev-conf-75b67853", "brief": {"key_ideas": ["DepSurf tool identifies dependency mismatches between eBPF programs and kernel internals across versions", "eBPF programs deeply integrate with unstable kernel internals creating compatibility challenges", "Quantifies the scale of eBPF-kernel coupling that causes program breakage on kernel updates", "Provides actionable data for both eBPF developers and kernel maintainers on stability risks"], "relevance": "The EuroSys companion to the earlier 'Revealing Unstable Foundations' entry — DepSurf is the actual tool. Directly addresses the Linux kernel's eBPF ABI stability challenge. Published at EuroSys.", "methodology": "EuroSys. eBPF dependency analysis across kernel versions. DepSurf tool implementation."}, "concepts": ["eBPF (Extended Berkeley Packet Filter)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "DepSurf: tool for eBPF-kernel dependency mismatch detection", "description": "DepSurf identifies dependency mismatches between eBPF programs and kernel internals, quantifying how kernel updates break deployed eBPF programs. Actionable stability data for the eBPF ecosystem."}]},

    {"ev_id": "ev-conf-ec1db6db", "brief": {"key_ideas": ["File system for the hierarchical edge — addresses data processing demands of multiplying IoT devices", "Traditional cloud computing cannot meet latency/bandwidth demands — edge processing needed", "PathFS provides a filesystem designed for the edge's hierarchical topology of device→edge→cloud", "Manages data across the edge hierarchy rather than treating each level independently"], "relevance": "Relevant to the kernel's filesystem infrastructure for edge computing. PathFS extends the VFS model for hierarchical edge topologies where data moves between device, edge, and cloud tiers. The kernel needs to support filesystems that span network boundaries — relevant to FUSE, NFS, and distributed filesystem support. Published at EuroSys.", "methodology": "EuroSys. Edge filesystem for hierarchical IoT topology. Device→edge→cloud data management."}, "concepts": ["Virtual Filesystem Switch", "FUSE (Filesystem in Userspace)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Hierarchical edge filesystem for IoT data management", "description": "PathFS manages data across the hierarchical edge topology (device→edge→cloud) rather than treating each tier independently. Addresses latency and bandwidth limitations of cloud-only processing."}]},

    {"ev_id": "ev-conf-7bbeec8c", "brief": {"key_ideas": ["Supporting hybrid virtualization orchestration for edge computing", "Microservice architectures need customized virtualization per functional block based on requirements", "Hybrid approach: mix VMs, containers, and unikernels within the same edge deployment", "Orchestrates diverse isolation levels across edge workloads based on their specific constraints"], "relevance": "Relevant to the kernel's role in managing heterogeneous isolation primitives. The kernel provides containers (namespaces/cgroups), VMs (KVM), and must interact with unikernels. Hybrid orchestration means the kernel must support dynamic selection of isolation mechanism per workload — sometimes a container suffices, sometimes a VM is needed. Published at EuroSys.", "methodology": "EuroSys. Hybrid virtualization for edge. Container/VM/unikernel orchestration."}, "concepts": ["KVM (Kernel-based Virtual Machine)", "Namespaces", "Control Groups (cgroups v2)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Hybrid VM/container/unikernel orchestration for edge computing", "description": "Orchestrates diverse isolation mechanisms (VMs, containers, unikernels) within the same edge deployment, selecting the appropriate level per workload based on specific requirements and constraints."}]},

    {"ev_id": "ev-conf-f17ce5a8", "brief": {"key_ideas": ["Real-time NFS performance metrics extraction for cloud environments", "NFS is commonly used but multi-tenant cloud makes distributed filesystems prone to instability", "TrackIops extracts per-client and per-operation NFS metrics in real-time", "Provides the observability needed to debug NFS performance issues in cloud deployments"], "relevance": "Relevant to the kernel's NFS server/client implementation and tracing infrastructure. Extracting per-operation NFS metrics likely uses the kernel's nfsd tracepoints and perf events. The multi-tenant instability problem is a kernel-level issue — the NFS server running in the kernel serves multiple clients. Published at EuroSys.", "methodology": "EuroSys. Real-time NFS metrics extraction. Multi-tenant cloud NFS performance analysis."}, "concepts": ["Virtual Filesystem Switch", "Perf Events Subsystem"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Real-time NFS performance metrics for multi-tenant cloud", "description": "TrackIops extracts per-client, per-operation NFS performance metrics in real-time, providing the observability needed to debug NFS instability in multi-tenant cloud environments."}]},

    {"ev_id": "ev-conf-49e9a1ab", "brief": {"key_ideas": ["VM rescheduling optimization via deep reinforcement learning for datacenter fragment management", "VM creation/release scatters resource fragments across physical machines requiring periodic rescheduling", "DRL learns optimal VM placement to consolidate fragments and improve resource utilization", "Targets industry-scale datacenters with large numbers of VMs"], "relevance": "Relevant to the kernel's KVM VM placement and cgroup resource management. VM rescheduling triggers live migration through KVM's migration infrastructure. The DRL optimization must account for kernel-level constraints: NUMA topology, memory bandwidth, CPU affinity. Published at EuroSys.", "methodology": "EuroSys. DRL-based VM rescheduling. Resource fragment consolidation. Datacenter-scale evaluation."}, "concepts": ["KVM (Kernel-based Virtual Machine)", "NUMA Topology and Memory Policy"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "DRL-optimized VM rescheduling for datacenter fragment management", "description": "Deep reinforcement learning optimizes VM placement to consolidate resource fragments scattered across physical machines, improving datacenter utilization through NUMA-aware live migration."}]},

    {"ev_id": "ev-conf-8842f8d4", "brief": {"key_ideas": ["Finds imbalance failures in distributed file systems using a load variance model", "Load balancing in DFS is crucial but finding imbalance failures is difficult", "Themis models load variance to detect when the DFS load distribution deviates from expected patterns", "Identifies root causes of load imbalance that degrade response times"], "relevance": "Relevant to the kernel's distributed filesystem support (NFS, Ceph, GlusterFS). Load imbalance in DFS servers manifests as uneven kernel resource usage (CPU, memory, I/O) across nodes. The load variance model could use the kernel's PSI (Pressure Stall Information) or cgroup statistics for detection. Published at EuroSys.", "methodology": "EuroSys. Distributed filesystem load variance modeling. Imbalance failure detection."}, "concepts": ["Virtual Filesystem Switch"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Load variance model for DFS imbalance failure detection", "description": "Themis detects load distribution deviations in distributed file systems using a variance model, identifying root causes of imbalance that degrade performance and response times."}]},

    {"ev_id": "ev-conf-ea2040a1", "brief": {"key_ideas": ["Already done in batch 016"], "relevance": ".", "methodology": "."}, "concepts": [], "claims": []},

    {"ev_id": "ev-conf-1de3f24a", "brief": {"key_ideas": ["Evaluates commonly used Kubernetes security scanning tools", "K8s security is essential in edge-cloud continuum — scanning tools help identify vulnerabilities", "Compares effectiveness of scanning tools for detecting K8s misconfigurations and vulnerabilities", "Provides guidance on which tools are most effective for different threat categories"], "relevance": "Relevant to the kernel's security posture in Kubernetes deployments. K8s scanning tools check container security configurations that map to kernel settings: seccomp profiles, capability drops, namespace isolation, cgroup limits. Understanding which tools are effective helps identify gaps in kernel-level container security. Published at EuroSys.", "methodology": "EuroSys. K8s security scanner evaluation. Misconfiguration and vulnerability detection comparison."}, "concepts": ["Seccomp-BPF", "Namespaces", "POSIX Capabilities"], "claims": [{"kind": "Observation", "id_prefix": "obs", "name": "K8s security scanners have uneven coverage of kernel security settings", "description": "Evaluation of commonly used Kubernetes security scanning tools reveals uneven effectiveness at detecting kernel-level security misconfigurations (seccomp profiles, capability drops, namespace isolation)."}]},

    {"ev_id": "ev-conf-74d28344", "brief": {"key_ideas": ["Warms serverless ML inference via inter-function model transformation", "Cold start in serverless ML containers causes long inference delays — currently mitigated by keeping warm instances", "Transforms a warmed model from one function to serve a different function's inference request", "Avoids cold starting a new container by reusing a warm container with model transformation"], "relevance": "Relevant to the kernel's container lifecycle and memory management for serverless. Model transformation reuses an existing container (warm memory, loaded kernel state) rather than creating a new one — the kernel saves container teardown + creation overhead. The model transformation happens in userspace but the kernel benefits from not managing container churn. Published at EuroSys.", "methodology": "EuroSys. Inter-function model transformation for ML inference warming. Cold start reduction."}, "concepts": ["Namespaces", "Control Groups (cgroups v2)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Inter-function model transformation to avoid serverless cold starts", "description": "Optimus reuses warm containers by transforming the loaded model to serve different inference requests, avoiding container cold start. The kernel benefits from reduced container lifecycle churn."}]},
]

def main():
    conn = sqlite3.connect(DB_PATH, timeout=30)
    conn.execute("PRAGMA journal_mode=WAL"); conn.execute("PRAGMA busy_timeout=10000")
    concepts_map = {}
    for r in conn.execute("SELECT id, json_extract(attrs, '$.name') FROM nodes WHERE kind = 'Concept'").fetchall():
        if r[1]: concepts_map[r[1].lower()] = r[0]
    stats = {"briefs": 0, "claims": 0, "edges": 0}
    for paper in ALL_PAPERS:
        ev_id = paper["ev_id"]; brief = paper["brief"]
        if not brief.get("key_ideas") or len(brief["key_ideas"]) < 2: continue
        existing = conn.execute("SELECT 1 FROM nodes rb JOIN edges re ON re.source_id = rb.id WHERE rb.kind = 'ResearchBrief' AND re.kind = 'extracted-from' AND re.target_id = ?", (ev_id,)).fetchone()
        if existing: print(f"  SKIP: {ev_id}"); continue
        title_row = conn.execute("SELECT json_extract(s.attrs, '$.title'), json_extract(s.attrs, '$.published_date') FROM edges se JOIN nodes s ON s.id = se.target_id WHERE se.kind = 'sourced-from' AND se.source_id = ?", (ev_id,)).fetchone()
        title = title_row[0] if title_row else ""; date = title_row[1] if title_row else "2025-01-01"
        brief_id = f"rb-{uuid.uuid4().hex[:12]}"
        conn.execute("INSERT INTO nodes (id, kind, attrs) VALUES (?, 'ResearchBrief', ?)", (brief_id, json.dumps({"title": title, "key_ideas": json.dumps(brief["key_ideas"]), "relevance": brief["relevance"], "methodology": brief["methodology"], "source_date": date or "2025-01-01", "artifact_class": "A"})))
        conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (brief_id, ev_id))
        stats["briefs"] += 1
        for cname in paper["concepts"]:
            cid = concepts_map.get(cname.lower())
            if cid:
                try: conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('summarizes-for', ?, ?, '{}')", (brief_id, cid)); stats["edges"] += 1
                except: pass
                try: conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (cid, ev_id)); stats["edges"] += 1
                except: pass
        for claim in paper["claims"]:
            claim_id = f"{claim['id_prefix']}-{uuid.uuid4().hex[:12]}"
            conn.execute("INSERT INTO nodes (id, kind, attrs) VALUES (?, ?, ?)", (claim_id, claim["kind"], json.dumps({"name": claim["name"], "description": claim["description"], "source_date": date or "2025-01-01"})))
            conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (claim_id, ev_id))
            stats["claims"] += 1; stats["edges"] += 1
        print(f"  OK: {title[:60].encode('ascii', 'replace').decode()}")
    conn.commit(); conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
    tb = conn.execute("SELECT count(*) FROM nodes WHERE kind = 'ResearchBrief'").fetchone()[0]
    tc = conn.execute("SELECT count(*) FROM nodes WHERE kind IN ('Problem','Observation','Proposal','PerformanceProfile','FailureMode','Benchmark')").fetchone()[0]
    ta = conn.execute("SELECT count(*) FROM nodes WHERE kind = 'ResearchBrief' AND json_extract(attrs, '$.artifact_class') = 'A'").fetchone()[0]
    print(f"\nCreated {stats['briefs']} briefs, {stats['claims']} claims, {stats['edges']} edges")
    print(f"Totals: {tb} briefs ({ta} Class A), {tc} claims in DB")
    conn.close()

if __name__ == "__main__":
    main()
