"""Deep enrichment batch 020: 15 papers — SOSP, ASPLOS remainder, and select EuroSys/NSDI.
"""
import json, sqlite3, uuid
DB_PATH = "data/master.db"

ALL_PAPERS = [
    {"ev_id": "ev-conf-2d4556cc", "brief": {"key_ideas": ["uProcess: pure userspace process abstraction enabling sub-microsecond core rescheduling without kernel traps", "Constructs a special privileged mode in userspace that enables CPU core reassignment among applications", "Eliminates the kernel scheduler from the critical path for latency-sensitive core allocation", "Sub-microsecond timescale rescheduling — orders of magnitude faster than kernel context switches"], "relevance": "Directly challenges the kernel scheduler's role in core allocation. uProcess moves scheduling decisions entirely to userspace, bypassing the kernel's CFS/EEVDF scheduler for latency-critical paths. This is the most aggressive user-space scheduling proposal — more extreme than sched_ext (which still runs in-kernel) or LibPreemptible (which uses hardware assists). Relevant to the kernel's context switch path, IPC mechanisms, and the future of kernel scheduling. Published at SOSP.", "methodology": "SOSP. Userspace process abstraction. Sub-microsecond core rescheduling. Kernel-bypass scheduling."}, "concepts": ["Scheduling Classes", "Process Creation (fork/clone)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Userspace process abstraction for sub-microsecond core scheduling", "description": "uProcess enables CPU cores to be rescheduled among applications at sub-microsecond timescale without trapping into the kernel. Constructs a privileged userspace mode that eliminates the kernel scheduler from the critical path."}]},

    {"ev_id": "ev-conf-01f1f017", "brief": {"key_ideas": ["Identifies kernel out-of-order concurrency bugs through in-vivo memory access reordering", "Kernel concurrency bugs from memory ordering are notoriously difficult to find — developers must reason about hardware reordering", "In-vivo approach: instruments the running kernel to actually reorder memory accesses, exposing bugs that manifest under real hardware reordering", "Goes beyond TSan/KCSAN which detect data races but not ordering-dependent bugs"], "relevance": "Directly targets the Linux kernel's most subtle bug class: concurrency bugs from memory ordering. The kernel's existing tools (KCSAN, lockdep) detect data races and lock violations but not bugs that depend on hardware memory reordering. OZZ fills this gap by actually reordering memory accesses in the running kernel. Published at SOSP — one of the most important kernel correctness papers.", "methodology": "SOSP. In-vivo memory access reordering. Kernel concurrency bug detection. Beyond data race detection."}, "concepts": ["Spinlock", "Read-Copy-Update"], "claims": [{"kind": "Problem", "id_prefix": "prob", "name": "Kernel memory ordering bugs undetectable by existing tools", "description": "Kernel concurrency bugs from hardware memory reordering are notoriously difficult to find. KCSAN detects data races but not ordering-dependent bugs. Developers must reason about CPU memory models which is error-prone."}, {"kind": "Proposal", "id_prefix": "prop", "name": "In-vivo memory reordering for kernel concurrency bug detection", "description": "OZZ instruments the running kernel to actually reorder memory accesses, exposing concurrency bugs that only manifest under specific hardware memory ordering. Goes beyond KCSAN/TSan's data race detection."}]},

    {"ev_id": "ev-conf-602fb86e", "brief": {"key_ideas": ["Tolerates partial failures in a CXL pod — multiple hosts sharing a CXL memory module", "A CXL pod enables porting single-host shared-memory programs to multi-host execution", "When one host in the pod fails, the shared CXL memory is at risk — Lupin provides fault tolerance", "Addresses the reliability gap when using CXL for multi-host shared memory"], "relevance": "Directly relevant to the kernel's CXL memory management for multi-host configurations. When multiple hosts share CXL memory, the kernel on each host must handle the failure of other hosts gracefully — the shared memory may be in an inconsistent state. Lupin provides the fault tolerance mechanisms the kernel needs to implement for production CXL pods. Published at SOSP.", "methodology": "SOSP. CXL pod fault tolerance. Multi-host shared memory reliability. Partial failure handling."}, "concepts": ["Adaptive CXL Memory Tiering", "NUMA Topology and Memory Policy"], "claims": [{"kind": "Problem", "id_prefix": "prob", "name": "CXL pod partial failure leaves shared memory inconsistent", "description": "When multiple hosts share CXL memory in a pod, the failure of one host can leave shared memory in an inconsistent state. Single-host shared-memory programs ported to CXL pods need fault tolerance that the current kernel CXL stack doesn't provide."}, {"kind": "Proposal", "id_prefix": "prop", "name": "Fault tolerance for multi-host CXL memory pods", "description": "Lupin tolerates partial host failures in CXL pods, ensuring shared CXL memory remains consistent when individual hosts fail. Enables reliable multi-host shared-memory applications on CXL."}]},

    {"ev_id": "ev-conf-fc5b0c36", "brief": {"key_ideas": ["Observable communication abstraction for learning-directed operating systems — a data-plane for efficient data movement in ML-augmented OS policies", "As ML catches up with evolving hardware/application demands, the OS needs a data-plane abstraction for policy data", "OQueue provides efficient data movement between OS components that implement ML-based policies", "Addresses the infrastructure gap for building ML-augmented kernel subsystems"], "relevance": "Proposes the data-plane infrastructure that ML-augmented kernel subsystems need. Papers like Aegis (learned scheduler), LearnedCache (eBPF ML eviction), and SchedCP (LLM scheduler) all need efficient data movement for ML policy decisions — OQueue provides this abstraction. Relevant to the kernel's tracing, perf events, and eBPF map infrastructure as the data channels for ML policies. Published at SOSP.", "methodology": "SOSP. Observable communication for ML-augmented OS. Data-plane abstraction. ML policy data movement."}, "concepts": ["Scheduling Classes", "eBPF (Extended Berkeley Packet Filter)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Data-plane abstraction for ML-augmented OS policy communication", "description": "OQueue provides efficient, observable data movement between OS components implementing ML-based policies. Addresses the infrastructure gap for learning-directed operating systems."}]},

    {"ev_id": "ev-conf-8ff518da", "brief": {"key_ideas": ["Applies Verus verification tool to a root-of-trust bootloader — proving correctness of the first code that runs on boot", "Verification in the target programming language (Rust) rather than requiring separate verification languages", "Root-of-trust bootloader is the foundation of the entire system security chain — bugs here undermine everything", "Demonstrates that modern verification tools are approachable enough for practical boot code"], "relevance": "Directly relevant to the kernel's boot security chain. The root-of-trust bootloader runs before UEFI and the kernel — if compromised, all higher-level security fails. Verifying it in Rust using Verus shows that formal verification is practical for the most critical boot-path code. This complements the Verified Rust Page Tables paper (batch 016) — both use Rust + verification for kernel-level code. Published at SOSP.", "methodology": "SOSP. Verus verification of Rust bootloader. Root-of-trust correctness proof."}, "concepts": ["EFI/UEFI Boot and Runtime Services"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Verified root-of-trust bootloader using Rust and Verus", "description": "Applies Verus verification to a Rust root-of-trust bootloader, proving correctness of the first code that runs on boot. Demonstrates modern verification tools are practical for the most critical boot-path code."}]},

    {"ev_id": "ev-conf-f46e741e", "brief": {"key_ideas": ["Efficient file-lifetime redundancy management for cluster file systems", "Data services change redundancy configs (transcode) over file lifetimes — but transcoding is I/O-intensive", "Morph optimizes redundancy transitions (e.g., replication → erasure coding) to reduce I/O cost", "Addresses the practical problem of changing data protection levels as data ages"], "relevance": "Relevant to the kernel's filesystem and block layer for distributed storage. Redundancy transitions (replication to erasure coding) generate massive I/O through the kernel's block layer and filesystem. Morph optimizes this path. Relevant to the kernel's MD (software RAID) and device-mapper which handle storage redundancy. Published at SOSP.", "methodology": "SOSP. File-lifetime redundancy management. Transcoding I/O optimization. Cluster filesystem evaluation."}, "concepts": ["Block Device Layer"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "I/O-efficient file redundancy transitions in cluster filesystems", "description": "Morph optimizes redundancy configuration changes (e.g., replication → erasure coding) over file lifetimes, reducing the massive I/O cost of transcoding in cluster file systems."}]},

    {"ev_id": "ev-conf-d1691cef", "brief": {"key_ideas": ["Pragmatic test-oracle specification for a production Android hypervisor using ghost state", "Full functional verification is feasible but has high barriers — pragmatic approach provides useful guarantees with lower effort", "Tests the hypervisor's security properties using specification-derived test oracles", "Targets a production hypervisor (Android pKVM) — not an academic prototype"], "relevance": "Directly tests the security of Android's production hypervisor (pKVM, which is based on KVM). The ghost-state test oracle approach provides security guarantees for KVM-derived hypervisors without full formal verification. Relevant to the kernel's KVM implementation which shares code with pKVM. Published at SOSP.", "methodology": "SOSP. Ghost-state test oracle for production hypervisor. Android pKVM security testing."}, "concepts": ["KVM (Kernel-based Virtual Machine)"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Pragmatic test-oracle specification for production KVM hypervisor", "description": "Ghost-state test oracles provide security guarantees for Android's production pKVM hypervisor with lower effort than full functional verification. Pragmatic approach to verifying KVM-derived hypervisor security."}]},

    {"ev_id": "ev-conf-a2d42b2c", "brief": {"key_ideas": ["NatureSSD: flash storage allocation leveraging natural resilience of ML applications", "Modern ML applications tolerate some data errors — exploits this to reduce SSD resource allocation (over-provisioning, error correction)", "QoS-aware SSD allocation that matches flash reliability to application tolerance", "Reduces SSD cost for containerized ML workloads by relaxing storage guarantees that ML doesn't need"], "relevance": "Proposes that the kernel's block layer should expose SSD reliability levels to applications. Currently the kernel treats all SSD blocks equally — NatureSSD suggests differentiated reliability tiers matched to application error tolerance. This requires kernel block layer changes to support QoS-differentiated storage allocation (similar to how cgroups provide CPU/memory QoS). Published at SOSP.", "methodology": "SOSP. QoS-aware SSD allocation for ML. Flash reliability vs application tolerance tradeoff."}, "concepts": ["Block Device Layer", "NVMe Driver Subsystem"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Reliability-differentiated SSD allocation for error-tolerant ML", "description": "NatureSSD matches flash storage reliability to ML application error tolerance, reducing SSD over-provisioning and cost. Exploits the natural resilience of ML applications that tolerate some data errors."}]},

    {"ev_id": "ev-conf-10412d7a", "brief": {"key_ideas": ["Speculative pipelined encryption for fast confidential LLM serving on NVIDIA H100", "GPU confidential computing encryption causes up to 52.8-88.2% throughput drop — PipeLLM reduces this", "Pipelines encryption with computation to hide encryption latency", "Targets the performance overhead of running LLMs inside NVIDIA H100 confidential computing"], "relevance": "Relevant to the kernel's interaction with NVIDIA H100 confidential computing mode. The kernel manages the confidential VM in which the GPU operates (via KVM + TDX/SEV). The encryption overhead that PipeLLM addresses is partly managed by the kernel's memory encryption path. Relevant to KVM's confidential computing GPU passthrough. Published at ASPLOS.", "methodology": "ASPLOS. Pipelined encryption for confidential GPU LLM serving. NVIDIA H100 evaluation."}, "concepts": ["KVM (Kernel-based Virtual Machine)"], "claims": [{"kind": "Problem", "id_prefix": "prob", "name": "GPU confidential computing encryption drops LLM throughput 52-88%", "description": "NVIDIA H100 confidential computing encryption causes 52.8-88.2% throughput degradation for LLM serving. The encryption overhead makes confidential GPU computing impractical for latency-sensitive inference."}, {"kind": "Proposal", "id_prefix": "prop", "name": "Speculative pipelined encryption for confidential LLM serving", "description": "PipeLLM pipelines encryption with GPU computation to hide encryption latency, reducing the throughput impact of confidential computing for LLM serving on NVIDIA H100."}]},

    {"ev_id": "ev-conf-36b007f6", "brief": {"key_ideas": ["Memory Disambiguation Unit (MDU) side channels break balanced branches in Intel SGX enclaves", "Software defenses against control-flow attacks on SGX are insufficient against MDU side channels", "Exploits a microarchitectural unit (MDU) that hasn't been previously used for side-channel attacks", "Affects Intel SGX enclaves — potentially leaking secret-dependent control flow"], "relevance": "Reveals a new microarchitectural side channel in Intel processors that affects SGX enclaves. The kernel's SGX driver creates and manages enclaves — new side channels mean the kernel's SGX isolation guarantees are weaker than assumed. The MDU is a microarchitectural unit the kernel cannot directly control. Published at ASPLOS.", "methodology": "ASPLOS. MDU microarchitectural side channel. Intel SGX attack. Control-flow leakage analysis."}, "concepts": ["Linux Security Modules"], "claims": [{"kind": "FailureMode", "id_prefix": "fail", "name": "Memory Disambiguation Unit side channel attacks on SGX enclaves", "description": "The Memory Disambiguation Unit (MDU) in Intel processors creates a previously unknown side channel that leaks secret-dependent control flow from SGX enclaves. Existing software defenses are insufficient against this microarchitectural leakage."}]},

    {"ev_id": "ev-conf-29ba1f3d", "brief": {"key_ideas": ["KV-SSD design supporting all workload types — not just large-value workloads that existing designs target", "Existing KV-SSDs tuned for large values perform poorly when key sizes are relatively large", "Re-evaluates KV-SSD design with unexplored workload types where key-value size ratios differ", "Provides a unified KV-SSD architecture handling diverse real-world KV workload patterns"], "relevance": "Relevant to the kernel's NVMe and block device interface for KV-SSDs. The Linux kernel is developing support for NVMe KV command sets — understanding which workload types KV-SSDs can handle efficiently informs kernel driver design. The key-size vs value-size tradeoff affects how the kernel should format and submit KV commands. Published at ASPLOS.", "methodology": "ASPLOS. KV-SSD architecture for diverse workloads. Key-value size analysis."}, "concepts": ["NVMe Driver Subsystem", "Block Device Layer"], "claims": [{"kind": "Observation", "id_prefix": "obs", "name": "KV-SSDs designed for large values fail on large-key workloads", "description": "Existing KV-SSD designs are tuned for workloads where values are much larger than keys. Real-world workloads with large keys expose different performance characteristics that require a redesigned KV-SSD architecture."}]},

    {"ev_id": "ev-conf-564f17d9", "brief": {"key_ideas": ["Already enriched in batch 015 as Snowplow"], "relevance": "Already done.", "methodology": "."}, "concepts": [], "claims": []},

    {"ev_id": "ev-conf-eb5573a1", "brief": {"key_ideas": ["Discovers better PCIe GPU communication paths through automated topology exploration", "GPU clusters communicating via PCIe have received limited study compared to NVLink/DGX systems", "TCCL finds communication paths that existing libraries miss, improving collective performance", "Targets the majority of GPU deployments which use PCIe, not proprietary interconnects"], "relevance": "Relevant to the kernel's PCIe topology management and GPU driver interaction. The kernel's PCIe subsystem exposes topology information that TCCL uses to find optimal communication paths. Better topology-aware GPU communication reduces kernel-mediated data transfer overhead. Published at ASPLOS.", "methodology": "ASPLOS. PCIe GPU communication topology exploration. Collective communication optimization."}, "concepts": [], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Topology-aware PCIe GPU collective communication optimization", "description": "TCCL discovers better communication paths for PCIe GPU clusters through automated topology exploration, improving collective performance on the majority of GPU deployments that use PCIe rather than proprietary interconnects."}]},

    {"ev_id": "ev-sosp25-aeolia", "brief": {"key_ideas": ["Fast and secure userspace interrupt-based storage stack — bypasses the kernel for storage I/O using user interrupts", "Eliminates kernel involvement in the storage I/O critical path via Intel User Interrupts (UIPI)", "Provides security through hardware-enforced isolation rather than kernel-mediated access control", "Demonstrates that the kernel's storage stack overhead is avoidable with new hardware features"], "relevance": "Directly challenges the kernel's storage stack by showing it can be bypassed entirely using Intel User Interrupts for completion notification. The kernel's block layer, NVMe driver, and io_uring are all in the storage I/O path — Aeolia removes them. This is the storage equivalent of DPDK (kernel bypass for networking). Relevant to the kernel's NVMe driver, io_uring, and the future of the kernel's role in storage I/O. Published at SOSP 2025.", "methodology": "SOSP 2025. Userspace storage stack with Intel User Interrupts. Kernel-bypass storage I/O. Security via hardware isolation."}, "concepts": ["NVMe Driver Subsystem", "io_uring", "Interrupt Handling"], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Kernel-bypass storage stack using Intel User Interrupts", "description": "Aeolia eliminates kernel involvement in storage I/O via Intel User Interrupts for completion notification. Hardware-enforced isolation replaces kernel-mediated access control. Demonstrates the kernel's storage stack overhead is avoidable."}]},

    {"ev_id": "ev-conf-6169b069", "brief": {"key_ideas": ["Tracing dependencies in collective communication for reliable LLM training", "Reliability issues in collective communication waste resources and degrade model performance", "Mycroft traces dependencies across collective operations to identify root causes of reliability failures", "Targets the gap where existing collective communication libraries don't provide dependency visibility"], "relevance": "Relevant to the kernel's RDMA and networking subsystems that underpin collective communication for LLM training. Reliability issues in collectives often originate from kernel-level networking problems (RDMA errors, TCP timeouts, NIC failures). Mycroft's dependency tracing helps identify whether the root cause is in the kernel networking stack. Published at SOSP.", "methodology": "SOSP. Collective communication dependency tracing. LLM training reliability. Root cause identification."}, "concepts": [], "claims": [{"kind": "Proposal", "id_prefix": "prop", "name": "Dependency tracing for reliable collective communication in LLM training", "description": "Mycroft traces dependencies across collective communication operations to identify root causes of reliability failures in LLM training, addressing the visibility gap in existing collective libraries."}]},
]

def main():
    conn = sqlite3.connect(DB_PATH, timeout=30)
    conn.execute("PRAGMA journal_mode=WAL"); conn.execute("PRAGMA busy_timeout=10000")
    concepts_map = {}
    for r in conn.execute("SELECT id, json_extract(attrs, '$.name') FROM nodes WHERE kind = 'Concept'").fetchall():
        if r[1]: concepts_map[r[1].lower()] = r[0]
    stats = {"briefs": 0, "claims": 0, "edges": 0}
    for paper in ALL_PAPERS:
        ev_id = paper["ev_id"]; brief = paper["brief"]
        if not brief.get("key_ideas") or len(brief["key_ideas"]) < 2: continue
        existing = conn.execute("SELECT 1 FROM nodes rb JOIN edges re ON re.source_id = rb.id WHERE rb.kind = 'ResearchBrief' AND re.kind = 'extracted-from' AND re.target_id = ?", (ev_id,)).fetchone()
        if existing: print(f"  SKIP: {ev_id}"); continue
        title_row = conn.execute("SELECT json_extract(s.attrs, '$.title'), json_extract(s.attrs, '$.published_date') FROM edges se JOIN nodes s ON s.id = se.target_id WHERE se.kind = 'sourced-from' AND se.source_id = ?", (ev_id,)).fetchone()
        title = title_row[0] if title_row else ""; date = title_row[1] if title_row else "2025-01-01"
        brief_id = f"rb-{uuid.uuid4().hex[:12]}"
        conn.execute("INSERT INTO nodes (id, kind, attrs) VALUES (?, 'ResearchBrief', ?)", (brief_id, json.dumps({"title": title, "key_ideas": json.dumps(brief["key_ideas"]), "relevance": brief["relevance"], "methodology": brief["methodology"], "source_date": date or "2025-01-01", "artifact_class": "A"})))
        conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (brief_id, ev_id))
        stats["briefs"] += 1
        for cname in paper["concepts"]:
            cid = concepts_map.get(cname.lower())
            if cid:
                try: conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('summarizes-for', ?, ?, '{}')", (brief_id, cid)); stats["edges"] += 1
                except: pass
                try: conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (cid, ev_id)); stats["edges"] += 1
                except: pass
        for claim in paper["claims"]:
            claim_id = f"{claim['id_prefix']}-{uuid.uuid4().hex[:12]}"
            conn.execute("INSERT INTO nodes (id, kind, attrs) VALUES (?, ?, ?)", (claim_id, claim["kind"], json.dumps({"name": claim["name"], "description": claim["description"], "source_date": date or "2025-01-01"})))
            conn.execute("INSERT INTO edges (kind, source_id, target_id, attrs) VALUES ('extracted-from', ?, ?, '{}')", (claim_id, ev_id))
            stats["claims"] += 1; stats["edges"] += 1
        print(f"  OK: {title[:60].encode('ascii', 'replace').decode()}")
    conn.commit(); conn.execute("PRAGMA wal_checkpoint(TRUNCATE)")
    tb = conn.execute("SELECT count(*) FROM nodes WHERE kind = 'ResearchBrief'").fetchone()[0]
    tc = conn.execute("SELECT count(*) FROM nodes WHERE kind IN ('Problem','Observation','Proposal','PerformanceProfile','FailureMode','Benchmark')").fetchone()[0]
    ta = conn.execute("SELECT count(*) FROM nodes WHERE kind = 'ResearchBrief' AND json_extract(attrs, '$.artifact_class') = 'A'").fetchone()[0]
    print(f"\nCreated {stats['briefs']} briefs, {stats['claims']} claims, {stats['edges']} edges")
    print(f"Totals: {tb} briefs ({ta} Class A), {tc} claims in DB")
    conn.close()

if __name__ == "__main__":
    main()
