================================================================================
IMPLEMENTATION PROMPT LIST
Venue Viewer, Venue Editor, and Retirement of /sources
================================================================================

Source plan:   docs/plans/venue-viewer-and-sources-replacement.md
Source audits: silk runs cb-audit-venue-viewer-01, cb-audit-venue-plan-02
               (both cb-audit, FINAL_STATUS SUCCESS, 6 stages, 0 violations)
Generated:     2026-09-14
Revised:       2026-09-15 (after PROMPTS 1 and 2 executed; decisions D-3/D-4/D-5
               answered by the operator; PROMPT 2B added; PROMPTS 3 and 6 widened)

EXECUTION ORDER: 1 [DONE] -> 2 [DONE] -> 2B -> 3 -> 4 -> 5 -> 6 -> 7
CRITICAL PATH:   4 -> 5 -> 6
PARALLELISABLE:  2B, 3 and 7 may run alongside 4 by another operator.

STATUS
  PROMPT 1   DONE 2026-09-14. No commit (measurement only).
  PROMPT 2   DONE 2026-09-14. Commits da66f41, 09bf666.
  PROMPT 2B  NEXT. Blocks every later prompt's "full suite green" exit criterion.
  PROMPT 3   Widened - see D-5.
  PROMPT 4   Blocked on D-1 and D-2.
  PROMPT 6   Widened - see D-4.

BLOCKING DECISIONS - must be answered before PROMPT 4 is issued:
  D-1  Venue representation: first-class node kind, or attribute + normalisation?
       No default. Full comparison in the plan, section 14.
       STATUS: OPEN.
  D-2  Authorisation for venue mutation: any logged-in user, or admin-only?
       Suggested default: admin-only for merge, any user for single-Source edit.
       STATUS: OPEN.

DECISIONS ANSWERED 2026-09-15 - raised by PROMPT 1's observed baseline:
  D-3  The `mcp>=1.0` pin floats to mcp 2.2.0, which renamed FastMCP to MCPServer.
       tests/test_e2e_pipeline.py and tests/test_mcp_server.py fail at import, and a
       collection error aborts the ENTIRE run, so plain `pytest` yields zero results.
       ANSWERED: MIGRATE src/mcp_server/server.py to the mcp 2.x MCPServer API.
       Do NOT pin mcp<2. Issued as PROMPT 2B.
  D-4  The three /radar tests fail. Investigated 2026-09-15: the radar PAGE IS NOT
       BROKEN. /radar was created 2026-06-29 (eb1a7d0) as a subsystem VULNERABILITY
       radar - heading "Subsystem Radar", subsystems with vuln and fix counts. It was
       rewritten 2026-07-15 (534d725 "rewrite radar as research-focused
       papers-by-concept view", then 5f08345 regrouping it subsystem/concept/paper).
       The tests were last touched 2026-07-09 (aaf455f), six days before the rewrite,
       and still assert the old semantics. Proof the page is sound: the
       radar_vuln_client fixture contains ZERO Source and ZERO Evidence nodes, and
       the current route requires the chain Concept -extracted-from-> Evidence
       -sourced-from-> Source(paper type), so the page correctly renders "0 research
       papers across 0 subsystems". Against real data the same query returns 1,433
       papers across 18 subsystems in data/master.db.
       ANSWERED: REWRITE the three tests against research-radar semantics with a
       fixture carrying papers. Do NOT change the page. Folded into PROMPT 6, which
       needs the same paper-chain fixture scaffolding for /venues.
       NOTE ON SCOPE: the venue viewer does NOT replace /radar. /radar stays exactly
       as it is. The venue viewer ADOPTS RADAR'S SHAPE in place of /sources.
  D-5  Two further declared-vs-actual drift failures, neither route-related:
       graph.rules.RULES_BY_KIND has no entry for ResearchBrief or HumanReview (two
       node kinds in NODE_KINDS with zero validation rules), and the /viz edgeColor
       map has no entry for 'summarizes-for' (an edge kind in EDGE_KINDS that renders
       uncoloured).
       ANSWERED: FOLD both into PROMPT 3, widening it from the SQLite CHECK
       constraint alone to declared-vs-actual drift across all three surfaces.

NOTE ON SKILL SELECTION
  /cb-fix is excluded per operator instruction (skill is broken). Work that would
  normally route to /cb-fix is issued as /cb-green below.
  PROMPT 1 is /cb-free and not /cb-ops: it needs `pip install`, which is NOT on the
  CB-OPS-INV-OPERATIONAL-ONLY allowlist (that allowlist covers npm install/ci and
  git operations only). It has no spec surface and cannot be a /cb-green.
  PROMPT 2 is the single grouped execution for all trivial/mechanical findings.


================================================================================
PROMPT 1
================================================================================

/cb-free — Establish a real test baseline (WP0)
            *** DONE 2026-09-14. No commit (measurement only). ***

  OUTCOME (observed, supersedes the prediction below):
    Install resolved to 38 packages, no conflicts, exactly as the dry run predicted.
    Plain `pytest -q` runs ZERO tests: tests/test_e2e_pipeline.py and
    tests/test_mcp_server.py fail at import (mcp 2.x renamed FastMCP to MCPServer)
    and a collection error aborts the whole run. This became D-3 / PROMPT 2B.
    With --continue-on-collection-errors: 68 failed, 1228 passed, 2 errors.
    tests/test_web.py holds 167 test functions, not 141 - the static count was
    wrong; 141 are top-level and 26 are inside TestDisplayNameForNode.
    The 68 failures resolve into four classes, not one:
      63  dead tests, deleted routes           -> PROMPT 2
       3  /radar, page sound, tests stale      -> D-4, PROMPT 6
       1  /viz edgeColor missing summarizes-for-> D-5, PROMPT 3
       1  RULES_BY_KIND missing 2 node kinds   -> D-5, PROMPT 3
       2  modules uncollectable (mcp 2.x)      -> D-3, PROMPT 2B
    Predicted 64 route-deletion failures, observed 63: six of the listed functions
    assert 404 and so were passing spuriously against the now-absent routes.

  - What: pytest is not installed in venv/ or system-wide, so the test suite has
    never been executed in this environment and every test-related finding in the
    plan is a STATIC derivation, not an observed result. Install the project with
    dev and ingest extras (`./venv/bin/pip install -e ".[dev,ingest]"`; a dry run
    resolved cleanly to 38 packages, no conflicts), then run `./venv/bin/pytest -q`
    and record the true pass/fail counts. Compare the observed result against the
    static prediction in plan section 2.6: 64 of 141 test functions in
    tests/test_web.py are predicted to fail because they call routes deleted on
    2026-07-29. Report the delta between predicted and observed. Do not fix
    anything in this prompt — this is measurement only. Note the environment
    already has node 22.23.1 (snap), npm 10.9.8, git 2.43.0 and a Linux-rebuilt
    better-sqlite3, all installed 2026-09-14.
  - Spec areas: none. No DAG read or write is required.
  - Code path: no source mutation. Touches venv/ only (gitignored).
  - Pre-commit hook: no commit. Nothing tracked changes.
  - Test: runs the full existing suite as the deliverable. Capture the exact
    counts and the list of failing test names for PROMPT 2.


================================================================================
PROMPT 2
================================================================================

/cb-green — Housekeeping: dead tests, README correction, encoding repair (WP1 + WP8)
            *** DONE 2026-09-14. Commits da66f41, 09bf666. ***
            *** silk run 97abb0a13fbd4b16, FINAL_STATUS SUCCESS, 0 violations. ***

  OUTCOME (observed, supersedes the figures below):
    (a) 69 test functions deleted, not 64. Resolved against the live FastAPI router
    rather than the static table: that reproduced the listed 64 exactly, and found 5
    more that call-site matching structurally CANNOT see because they make no dead
    call - they GET "/" and then assert a nav or dashboard link to a deleted page:
    test_dashboard_kind_links, test_nav_has_code_link, test_dashboard_links_to_vulns,
    test_nav_has_vulns_link, test_research_nav_link_visible. 64 + 5 = 69 reconciles
    exactly with the 63 observed route-class failures (6 of the 64 assert 404 and so
    were passing spuriously).
    TWO FIXTURE CORRECTIONS to the instruction below:
      paginated_client  KEPT. The claim that it "is used only by the pagination tests
                        and dies with them" is FALSE - test_sources_pagination still
                        uses it, and that test is explicitly retained. Deleting the
                        fixture would have broken it. It is now that test's SOLE user,
                        so PROMPT 6 must delete fixture and test together.
      research_client   DELETED. Genuinely orphaned (13 users, 0 survivors). Not named
                        in the instruction below.
    Also removed 5 section comments left heading no code. Comments still heading live
    code were left alone even where they cite an absent node (e.g. "# --- INV-KK-WEB-
    PAGINATION tests ---" above paginated_client) - retitling those is PROMPT 7.
    tests/test_web.py: 167 -> 98 tests. ruff clean. Exit assertion passes: all 66
    surviving HTTP calls resolve against the 30 registered routes.
    (c) ENCODING WAS 26 FILES, NOT 2. A tree-wide scan found the UTF-8 BOM in 26
    tracked files and mojibake in 21 (24 characters: 22 em dashes, 2 rightwards
    arrows). All repaired; re-scan reports zero of both. .gitattributes added.
    EXIT CRITERION NOT MET, BY DESIGN. `pytest -q` is not green: 5 failed, 1222
    passed, 2 errors. Every residual is out of scope and routed to D-3/D-4/D-5.

  - What: three mechanical, no-design-decision repairs grouped into one execution.
    (a) DEAD TESTS. Five commits on 2026-07-29 deliberately removed whole page
    families and cleaned src/ and templates correctly, but never touched the tests:
    275daea (Research page), 648bee9 (Vulns list/detail/API), 4271c0a (Subsystems),
    67c4c99 (All Nodes list), 27bf731 (Code Examples). tests/test_web.py last
    changed in aaf455f on 2026-07-09 and has had zero commits since, so it has been
    red for 47 days. 68 of its 138 HTTP calls target routes that no longer exist,
    across 64 of 141 test functions. Delete those 64 test functions and any fixture
    left unused by the deletion — note `paginated_client` at tests/test_web.py:822-840
    is used only by the pagination tests and dies with them; verify each fixture
    before removing it. The deleted route families are: GET /concepts (list, 19
    calls), GET /research and /research/{id} (22), GET /vulns, /vulns/{id} and
    /api/vuln-impact/{id} (19), GET /code-examples (3), GET /subsystems (1). The
    complete list of the 64 function names is in plan section 2.6 — use it verbatim.
    DO NOT delete `test_sources_pagination` (tests/test_web.py:925-928) — it targets
    /sources which still exists and is retired later, in PROMPT 6. DO NOT delete
    `test_no_write_endpoints` (tests/test_web.py:94-97) — it asserts POST / and
    PUT/DELETE /concepts/concept-1 return 405, which remains true.
    (b) README. README.md line 257 claims "326 tests, zero failures." Replace with
    the true count observed in PROMPT 1.
    (c) ENCODING. src/web/app.py and src/graph/engine.py both begin with a UTF-8 BOM
    and contain mojibake where an em dash was intended — both docstrings read
    "a-circumflex-euro-dagger" in place of the dash. Strip the BOM, repair the dash,
    and add a .gitattributes with `* text=auto eol=lf`. Root cause is a Windows round
    trip: the same defect produced a whole-tree LF->CRLF flip (43,417 insertions /
    43,417 deletions with `git diff --ignore-all-space` empty) and a Windows PE32+
    better_sqlite3.node binary, both corrected on 2026-09-14.
  - Spec areas: no new or modified DAG nodes. The P3 reason to submit is: this
    changeset deletes tests for routes that were removed in 2026-07-29 and repairs
    file encoding; it introduces no behaviour, and the spec ids cited by the deleted
    tests (INV-KK-WEB-PAGINATION, INV-KK-WEB-KIND-FILTER, ALG-KK-WEB-CODE-BROWSE,
    ALG-KK-WEB-GRAPH-VIZ, ALG-KK-WEB-RESEARCH-LIST, ALG-KK-WEB-QUERY-ROUTES,
    ALG-KK-WEB-KIND-DETAIL, and the malformed INV-KK-WEB-CODE-BROWSE- at
    tests/test_web.py:981) are already absent from the DAG, so no node is orphaned by
    the deletion. Confirm with `ril query nodes --filter KK` before submitting.
    DO NOT touch INV-KK-WEB-READ-ONLY here — it is handled in PROMPT 7.
  - Code path: tests/test_web.py (delete 64 functions + orphaned fixtures),
    README.md:257, src/web/app.py (BOM + line 1 docstring), src/graph/engine.py
    (BOM + line 1 docstring), .gitattributes (new file).
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors). Commit as two
    logical commits per CLAUDE.md rule 7: "test: remove tests for pages deleted on
    2026-07-29" covering (a) and (b), then "chore: strip BOM, repair mojibake, add
    .gitattributes" covering (c). Include .gitattributes. Git identity is not
    configured on this machine — use
    `-c user.name='reinier' -c user.email='reiniertl@gmail.com'`.
  - Test: no new tests. Exit criterion is `./venv/bin/pytest -q` green with an
    honest test count. Assert no remaining test in tests/test_web.py calls a route
    absent from the set registered in src/web/routes.py.


================================================================================
PROMPT 2B
================================================================================

/cb-green — Migrate the MCP server to the mcp 2.x MCPServer API (D-3)
            *** BLOCKS EVERY LATER PROMPT'S "FULL SUITE GREEN" EXIT CRITERION. ***

  - What: pyproject.toml declares `mcp>=1.0`, which currently resolves to mcp 2.2.0.
    mcp 2.x renamed FastMCP to MCPServer and moved it from mcp.server.fastmcp to
    mcp.server.mcpserver. src/mcp_server/server.py:25 still does
    `from mcp.server.fastmcp import FastMCP`, so importing the module raises
    ModuleNotFoundError. Two test modules import it - tests/test_mcp_server.py:14
    (`import mcp_server.server as srv`) and tests/test_e2e_pipeline.py:31
    (`from mcp_server.server import init_snapshot`) - and a COLLECTION error aborts
    the entire pytest run, so plain `pytest` currently yields zero test results for
    the whole repository, not just these two modules. This is why README.md now
    documents `pytest --continue-on-collection-errors` as a temporary workaround;
    remove that workaround paragraph as part of this change.
    The operator's decision (D-3) is MIGRATE. Do NOT pin mcp<2.
    The migration surface is small and has been probed against the installed 2.2.0:
      - line 25:  from mcp.server.fastmcp import FastMCP
                  -> from mcp.server.mcpserver import MCPServer
      - line 40:  mcp = FastMCP("know_kernel")
                  -> mcp = MCPServer("know_kernel")
                  MCPServer.__init__ takes `name` as its first positional parameter,
                  so the call form is unchanged.
      - lines 88-518: 18 @mcp.tool() decorators. MCPServer.tool() exists and, VERIFIED
                  BY PROBE, still returns the ORIGINAL FUNCTION rather than a Tool
                  wrapper - a decorated function remains directly callable and returns
                  its normal value. This matters because tests/test_mcp_server.py calls
                  the tools directly (srv.search_concepts("Queue"), srv.get_concept(...),
                  srv.list_subsystems(), ...) rather than through the protocol. Confirm
                  the probe holds rather than assuming it.
      - line 608: mcp.run() - MCPServer.run() exists. Confirm the transport default
                  is unchanged, since `kk-mcp` (a console script in pyproject) calls
                  main() at line 595.
    Read the migration guide before changing anything:
    https://py.sdk.modelcontextprotocol.io/v2/migration/#fastmcp-renamed-to-mcpserver
    Check the whole file for any other 1.x-only API, not just the four sites above -
    the rename is the error you can see, but 2.x changed other APIs too.
    Consider whether pyproject.toml should pin `mcp>=2` once migrated, so the
    declaration matches the code instead of floating across a breaking rename. That
    is a decision - ASK before changing the pin.
  - Spec areas: MOD-KK-MCP if it exists; query `ril query nodes --filter KK` and
    `ril query module-context MOD-KK-MCP --json` to find the real module id and any
    ALG-KK-MCP-* nodes citing the server. If the spec names FastMCP or the 1.x import
    path anywhere, those nodes must be updated - that is a genuine spec change and
    must NOT be reported as "no spec surface". If the spec is silent on the library,
    submit a P3 reason saying so explicitly.
  - Code path: src/mcp_server/server.py (lines 25, 40, 595-608, plus any other 1.x
    API found), README.md (remove the --continue-on-collection-errors paragraph added
    by PROMPT 2), pyproject.toml only if the pin decision says so.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors). Commit as
    "fix: migrate MCP server to the mcp 2.x MCPServer API" with any .cbm mutation
    files alongside. Git identity is not configured on this machine — use
    `-c user.name='reinier' -c user.email='reiniertl@gmail.com'`.
  - Test: exit criterion is that PLAIN `./venv/bin/pytest -q` collects successfully
    with no --continue-on-collection-errors flag, and that the 2 previously
    uncollectable modules run. Expected result after this prompt: 3 failed, 1224+
    passed, 0 errors - the 3 residual failures being the /radar tests (D-4, PROMPT 6)
    and the 2 drift failures (D-5, PROMPT 3); confirm the exact numbers rather than
    assuming them. All 25 tests in tests/test_mcp_server.py must pass unmodified;
    if any needs changing, that is a signal the migration changed behaviour and must
    be reported, not silently accommodated.


================================================================================
PROMPT 3
================================================================================

/cb-green — Reconcile declared schema with actual state, 3 surfaces (WP3 + D-5)

  - What: THREE declared-vs-actual drift surfaces, all the same defect class: a
    declaration in src/graph/schema.py that some other artifact has silently fallen
    out of step with. Surfaces (2) and (3) were found by PROMPT 1's observed baseline,
    are each a currently-failing test, and were folded in here by operator decision
    D-5. Fix all three.

    (2) RULES_BY_KIND drift. graph.rules.RULES_BY_KIND has NO entry for the node kinds
    `ResearchBrief` and `HumanReview`, both of which are in schema.NODE_KINDS. Two
    node kinds therefore carry zero validation rules. Verified directly, not only via
    the test: `set(NODE_KINDS) - set(RULES_BY_KIND) == {'HumanReview', 'ResearchBrief'}`.
    Failing test: tests/test_graph_rules.py::test_rules_by_kind_covers_all_node_kinds,
    which asserts `set(RULES_BY_KIND.keys()) == set(NODE_KINDS)`. Decide whether each
    kind gets real rules or an explicit empty-rule entry - if empty, the emptiness must
    be deliberate and commented, not incidental. That is a design decision - ASK.

    (3) /viz edgeColor drift. The edgeColor map rendered by the /viz route
    (src/web/routes.py:1040) has no entry for the edge kind `summarizes-for`, which is
    in schema.EDGE_KINDS, so that edge renders uncoloured. Failing test:
    tests/test_web.py::test_web_viz_edge_color_covers_all_edge_kinds, which asserts
    every kind in EDGE_KINDS appears in the rendered page.

    Consider whether ONE invariant should cover all three surfaces - "every kind
    declared in schema.py is honoured by every consumer of that declaration
    (SQL CHECK, RULES_BY_KIND, viz edgeColor)" - rather than three unrelated patches.
    A single predicate is what stops a fourth consumer drifting next. That is a design
    decision - ASK before choosing.

    (1) THE SQLITE CHECK CONSTRAINT. src/graph/schema.py SCHEMA_SQL declares `nodes.kind TEXT NOT NULL CHECK
    (kind IN (...27 kinds...))` and an equivalent CHECK on edges.kind. The live
    data/master.db actually holds `CREATE TABLE "nodes" (id TEXT PRIMARY KEY, kind
    TEXT NOT NULL, attrs TEXT NOT NULL DEFAULT '{}')` with NO CHECK at all; the
    quoted table name indicates the table was rebuilt at some point and the
    constraint was dropped. edges has likewise lost its CHECK. src/web/app.py runs
    `conn.executescript(SCHEMA_SQL)` on every startup but every statement is CREATE
    TABLE IF NOT EXISTS, so it can never repair an existing table, and there is no
    migration mechanism anywhere despite schema.py's docstring claiming "SQLite
    schema definition and migrations". Consequence: every pytest fixture and every
    :memory: app gets the CHECK while production does not, so the two disagree about
    which node kinds are legal. This blocks PROMPT 6 under D-1 Option A — inserting
    kind='Venue' would raise IntegrityError in tests and succeed in production.
    Note also that graph.engine.add_node does NOT validate kind in Python (it checks
    only REQUIRED_ATTRS), so the SQL CHECK is the sole kind enforcement; by contrast
    graph.engine.add_edge DOES validate in Python against EDGE_VALID_PAIRS and raises
    ValueError("Unknown edge kind: ...").
    Reconcile the two. Whether that means adding a real migration step or changing
    the declaration is a design decision — ASK before choosing.
  - Spec areas: MOD-KK-GRAPH (currently holds only 3 nodes:
    ALG-KK-GRAPH-CLASSIFY-SOURCE-MOTIVATIONS, INV-KK-MOTIV-EVIDENCE-GROUNDED,
    INV-KK-MOTIV-LABEL-VOCABULARY). Expect to author a new invariant asserting that
    the declared schema and the live database agree on legal node and edge kinds,
    and possibly an algorithm node for the migration path. Surface (3) also touches
    MOD-KK-WEB via the /viz route. If one unifying invariant is chosen over three
    patches, it belongs in MOD-KK-GRAPH and MOD-KK-WEB must depend on it. Query
    `ril query module-context MOD-KK-GRAPH --json` and
    `ril template invariant --json` for authoring rules.
  - Code path: src/graph/schema.py (SCHEMA_SQL, NODE_KINDS, EDGE_KINDS, init_db),
    src/web/app.py lines 26-35 (the lifespan executescript call),
    src/graph/engine.py add_node if Python-side kind validation is chosen,
    src/graph/rules.py RULES_BY_KIND for surface (2), and the edgeColor map reached
    from src/web/routes.py:1040 (/viz) for surface (3) - it may live in the viz
    template rather than in routes.py; locate it before editing.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors) and the full pytest
    suite. Commit as "fix: reconcile declared schema with live database" with the
    .cbm mutation files alongside the code change. Git identity is not configured on
    this machine — use `-c user.name='reinier' -c user.email='reiniertl@gmail.com'`.
  - Test: new tests asserting a fresh DB and data/master.db accept and reject the
    same kind values. Assert the migration (if chosen) is idempotent and re-runnable.
    Existing tests in tests/test_graph_engine.py and tests/test_graph_schema.py must
    pass unmodified. The two currently-red tests must go green WITHOUT being edited -
    tests/test_graph_rules.py::test_rules_by_kind_covers_all_node_kinds and
    tests/test_web.py::test_web_viz_edge_color_covers_all_edge_kinds. They are correct
    as written; they are detecting real drift. Weakening either to make it pass is a
    direct violation of CLAUDE.md rule 2 (the spec verifies the code, not the reverse).


================================================================================
PROMPT 4
================================================================================

/cb-green — Model venue representation and mutation authorisation (WP2 + WP6)
            *** SPEC-ONLY. NO CODE. REQUIRES D-1 AND D-2 ANSWERED FIRST. ***

  - What: venue has no referent in the model and must be given one before any UI or
    editor work. Facts, all verified against data/master.db on 2026-09-14: venue is
    a free-text key inside the JSON attrs blob of Source nodes; 3,574 Source nodes
    exist; 3,467 carry a venue (97.0%); 107 do not; there are 34 distinct venue
    strings. Venue is absent from every enforcement point in src/graph/schema.py —
    NODE_KINDS (27 kinds), EDGE_KINDS (37 kinds), EDGE_VALID_PAIRS, REQUIRED_ATTRS
    (Source requires only url, source_type, license), ID_PREFIXES, DATE_ATTRS — and
    from rules.RULES_BY_KIND, and there is no Venue node kind or venue edge kind in
    the spec DAG. No code in src/ writes Source.venue: src/ingest/batch_score.py:161
    and :167-168 read it for display, src/ingest/abstract_fetcher.py:108 mentions it
    in prose, and data/fetch_abstracts.py:47,53-55,66 is a one-off script that reads
    and filters on it. The 3,467 values were written by ad-hoc scripts in data/, so
    there is no existing writer to extend and the editor is genuinely new code.
    The values are unnormalised, which makes a raw GROUP BY produce wrong buckets.
    Known collisions: OSDI 2026 (136) / OSDI (30) / OSDI 2025 (1); SOSP (49) /
    SOSP 2025 (5); EuroSys (60) / EuroSys 2025 (poster) (1); ASPLOS (46) /
    ASPLOS 2025 (2); USENIX ATC (60) / USENIX ATC 2025 (1). The four arXiv cs.*
    values (cs.DC 897, cs.AR 888, cs.CR 705, cs.OS 356) cover 2,846 Sources — 82% of
    the corpus — and are arXiv categories, not venues; arXiv (19) and arXiv/PVLDB (1)
    are further variants. The 107 Sources with no venue break down by source_type as
    kernel-doc 43, article 22, preprint 17, vulnerability-database 16, discourse 9,
    and need an explicitly specified placement. The full 34-row distribution table is
    in plan section 2.5 and is the test oracle for PROMPT 6.
    Beware a name collision: INV-KK-FEED-CONFIG-VENUE-TYPE is the only venue-aware
    node in the DAG and constrains venue_type in data/feed_configs.json to a ten-value
    vocabulary (conference, journal, workshop, symposium, news, aggregator,
    mailing-list, preprint, documentation, vulnerability-db). That field is unrelated
    to Source.venue. feed_configs.json also carries "feed_type": "venue" on many
    entries, a third distinct meaning. Record the distinction, or rename.
    Also settle D-2 here. The gate authenticates every mounted request
    (INV-KK-AUTH-GATE-COVERS-MOUNT) so authentication is free, but require_admin at
    src/authgate/app.py:59-68 is called only by the four /admin/* handlers and NO
    MOD-KK-WEB mutating endpoint has any role check. Blast radius argues for one:
    PUT /api/abstract/{id} touches 1 Source, POST /api/review/{id} touches 1
    HumanReview plus 1 edge, but a venue merge of OSDI 2026 into OSDI would rewrite
    up to 897 Sources.
  - Spec areas: MOD-KK-GRAPH and MOD-KK-WEB. Under D-1 Option A expect IFC-KK-VENUE
    (fields name, venue_type, series, aliases), INV-KK-VENUE-NAME-UNIQUE,
    INV-KK-VENUE-SOURCE-EDGE, ALG-KK-VENUE-BACKFILL, plus schema additions:
    NODE_KINDS += Venue, EDGE_KINDS += published-at,
    EDGE_VALID_PAIRS["published-at"] = ("Source","Venue"), REQUIRED_ATTRS["Venue"],
    ID_PREFIXES["Venue"] = "venue-", RULES_BY_KIND["Venue"]. Under either option
    expect INV-KK-VENUE-NORMALISED (canonical form; the OSDI/SOSP/EuroSys/ASPLOS/ATC
    collisions resolve to one venue each) and INV-KK-VENUE-MUTATION-AUTHORISED (per
    D-2). Existing nodes to read first: INV-KK-FEED-CONFIG-VENUE-TYPE,
    INV-KK-WEB-MUTATION-ALLOWLISTED, INV-KK-AUTH-GATE-COVERS-MOUNT. Use
    `ril query module-context MOD-KK-GRAPH --json`,
    `ril query module-context MOD-KK-WEB --json`,
    `ril query impact-analysis MOD-KK-WEB MOD-KK-GRAPH --json` and
    `ril template <kind> --json`.
  - Code path: NONE in this prompt. Spec authoring via `ril apply-batch` only. The
    schema.py edits listed above land in PROMPT 6, not here.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors, no increase in the
    1,055 warning baseline attributable to the new nodes). Commit the .cbm mutation
    files as "spec: model venue representation". No source files in this commit.
  - Test: none — spec-only. Verify with `ril query node-info <new-ids> --json` that
    every authored node carries predicate-nl and the intended edges.


================================================================================
PROMPT 5
================================================================================

/cb-green — Author the venue viewer and editor spec nodes (WP4)
            *** SPEC-ONLY. NO CODE. DEPENDS ON PROMPT 4. ***

  - What: author the MOD-KK-WEB nodes that the implementation in PROMPT 6 will be
    written against. Three algorithms are needed: ALG-KK-WEB-VENUES (the viewer),
    ALG-KK-WEB-VENUE-EDIT (single-Source venue write) and ALG-KK-WEB-VENUE-MERGE
    (alias/merge). Model the viewer node on ALG-KK-WEB-RADAR, whose description is
    "Render /radar: query research papers joined to Concepts via Evidence, resolve
    each Concept's Subsystem, group into Subsystem -> Concept -> Papers hierarchy.
    Sort subsystems and concepts by paper count descending." The venue equivalent
    groups Venue -> Papers. Model the editor nodes on ALG-KK-WEB-ABSTRACT-EDIT,
    which is the established shape for a human-driven write to a Source attribute.
    Postconditions must cover: grouping by canonical venue, ordering, the nesting of
    papers inside a venue entry, and the explicit placement of the 107 Sources that
    carry no venue (an Unattributed bucket or exclusion by source_type — do not leave
    this implicit; silently dropping them loses 3% of the corpus from a page whose
    job is coverage).
    Attach INV-KK-WEB-QUERY-BOUNDED to ALG-KK-WEB-VENUES with a satisfies edge. That
    invariant is strength=strong, enforcementBasis=structural, predicate "forall
    route in web_list_routes. count(enrichment_calls(route)) <= route.per_page", and
    it is currently reported by spec:check as AUTH-WARN-ORPHANED-INV — wiring it here
    clears one of the 8 standing MOD-KK-WEB/MOD-KK-GRAPH warnings. The bar it sets is
    ALG-KK-WEB-RADAR's query discipline: exactly three queries regardless of corpus
    size (one join for source/concept pairs, one batched IN(...) subsystem lookup,
    one batched _batch_review_status call at src/web/routes.py:104-133), plus pure
    Python classification with no DB access (_classify_concept_motivations_fast at
    src/web/routes.py:827-859). The anti-pattern to avoid is /feed at
    src/web/routes.py:393-490, which calls _get_concept_subsystems and
    classify_source_motivations inside a per-item loop and satisfies the invariant
    only by the letter.
    Author INV-KK-VENUE-MUTATION-AUTHORISED per the D-2 answer from PROMPT 4, and
    IFC-KK-VENUE-EDIT for the result shape the domain functions return.
    Attach artifacts with `ril associate` at authoring time: 0 of the 25 existing
    MOD-KK-WEB nodes carry a source location and only 7 of 25 carry artifact
    associations, all 25 are riStatus=unchecked and predicateAuthority=nl-only. Do
    not extend that pattern.
  - Spec areas: MOD-KK-WEB. New: ALG-KK-WEB-VENUES, ALG-KK-WEB-VENUE-EDIT,
    ALG-KK-WEB-VENUE-MERGE, IFC-KK-VENUE-EDIT, INV-KK-VENUE-MUTATION-AUTHORISED.
    Modified by new edges: INV-KK-WEB-QUERY-BOUNDED (satisfies edge from the viewer),
    INV-KK-WEB-MUTATION-ALLOWLISTED (the editor routes fall under it). Read first:
    `ril query node-info ALG-KK-WEB-RADAR ALG-KK-WEB-ABSTRACT-EDIT
    INV-KK-WEB-QUERY-BOUNDED INV-KK-WEB-MUTATION-ALLOWLISTED --json`,
    `ril query module-context MOD-KK-WEB --json`, `ril template algorithm --json`.
    MOD-KK-WEB currently holds 15 algorithms and 10 invariants; the full list is in
    plan section 2.13.
  - Code path: NONE. Spec authoring via `ril apply-batch` only.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors and one fewer
    AUTH-WARN-ORPHANED-INV). Commit the .cbm files as "spec: author venue viewer and
    editor nodes". No source files.
  - Test: none — spec-only. Verify the satisfies edge landed with
    `ril query node-info INV-KK-WEB-QUERY-BOUNDED --json`.


================================================================================
PROMPT 6
================================================================================

/cb-green — Implement the venue viewer, the venue editor, and retire /sources (WP5)
            *** DEPENDS ON PROMPTS 2, 3, 4 AND 5. ***

  - What: write the code against the nodes authored in PROMPT 5.
    REMOVE. src/web/routes.py:240-261 is the sources_list handler: query params page
    (>=1) and per_page (10-200, default 50), a single
    "SELECT id, kind, attrs FROM nodes WHERE kind = 'Source' ORDER BY id LIMIT ?
    OFFSET ?" with per_page+1 to compute has_next, display_name resolution, rendering
    concept_list.html with title="Sources", active_kind="Source",
    all_kinds=_all_kinds(conn). It has no docstring and no spec citation, unlike every
    peer route, so there is no node to supersede. Delete it. Delete
    src/web/templates/concept_list.html — it is rendered by exactly one route, this
    one, verified by grep across src/ and tests/; its kind-filter dropdown at lines
    5-11 is already a dead control because the onchange navigates to ?kind=VALUE and
    sources_list ignores the parameter and hardcodes kind='Source'. Delete the nav
    entry at src/web/templates/base.html:45. Delete test_sources_pagination, now at
    tests/test_web.py:780 (line numbers shifted when PROMPT 2 removed 69 functions),
    AND the paginated_client fixture at tests/test_web.py:759, which after PROMPT 2 has
    test_sources_pagination as its SOLE remaining user and is orphaned by its removal.
    Verify that rather than assuming it.
    REPAIR THE RADAR TESTS (D-4). The three /radar tests are red and the PAGE IS NOT
    BROKEN - see D-4 in the header for the full evidence. /radar was rewritten on
    2026-07-15 (534d725, 5f08345) from a subsystem VULNERABILITY radar into a research
    papers-by-concept view; the tests were last touched 2026-07-09 and still assert the
    old semantics ("Subsystem Radar" heading, Scheduler/Memory Management rows carrying
    vuln and fix counts). The current heading is "Research Radar"
    (src/web/templates/radar.html:3). The radar_vuln_client fixture at
    tests/test_web.py:968 contains ZERO Source and ZERO Evidence nodes, so the page
    correctly renders "0 research papers across 0 subsystems" and the assertions find
    nothing. REWRITE the three tests - test_radar_returns_200:1046,
    test_radar_shows_subsystems_with_concepts:1052, test_radar_shows_vuln_and_fix_counts:1062
    - against research-radar semantics, backed by a fixture carrying the full chain
    Concept -extracted-from-> Evidence -sourced-from-> Source(source_type in
    'paper','preprint','conference-paper','conference-proceedings'). DO NOT CHANGE THE
    PAGE. Against real data the same query returns 1,433 papers across 18 subsystems in
    data/master.db, so the route is sound. This is folded into PROMPT 6 rather than
    given its own execution because /venues needs the IDENTICAL paper-chain fixture
    scaffolding - build one and share it.
    SCOPE NOTE: the venue viewer does NOT replace /radar. /radar stays. The venue
    viewer ADOPTS RADAR'S SHAPE in place of /sources.
    ADD. A new domain module src/ingest/venue_store.py that is the SOLE writer of
    venue data, mirroring src/ingest/source_abstract.py exactly: a module docstring
    naming the interface node and stating why one place owns the write, a frozen
    vocabulary constant, @dataclass result types, and functions that validate before
    writing via graph.engine.update_node_attrs. Provide set_venue(conn, source_id,
    venue, ...), merge_venues(conn, from_venue, to_venue) and a pure
    normalise_venue(raw) -> str with no DB access. CRITICAL CARVE-OUT: do NOT re-run
    validate_node after the write. set_abstract documents the reason and the venue
    editor hits it identically — rules.check_source_has_advisory requires every Source
    to have an Advisory, which the great majority of real Sources do not satisfy, so
    revalidating would reject a good venue edit for an unrelated pre-existing gap.
    Venue is an optional attr and the write cannot make a valid Source invalid. Also
    do NOT add venue to REQUIRED_ATTRS["Source"] — 107 Sources have no venue and
    adding it there would invalidate every existing Source and every add_node call in
    the ingest pipeline, which is the same reasoning source_abstract.py gives for the
    abstract fields.
    Add GET /venues rendering a new src/web/templates/venues.html. Model the template
    on src/web/templates/radar.html (117 lines): a two-level expand/collapse with a
    venue row carrying paper count, a hidden <tr> per venue containing the paper
    table (title link to /paper/{source_id}, external "source ->" link, review badge,
    type badge, date), and inline toggle JS keyed on a slugged id built like
    radar's sub.name|replace(' ','-')|replace('/','-').
    Add PUT /api/venue/{source_id:path} and POST /api/venue/merge as THIN routes that
    delegate to venue_store and map ValueError to 404/422, exactly as
    edit_abstract at src/web/routes.py:1156-1180 delegates to set_abstract.
    Add "/api/venue/" to WEB_MUTATION_ALLOWLIST at src/web/routes.py:31-36. That tuple
    IS the structural enforcement of INV-KK-WEB-MUTATION-ALLOWLISTED
    (invariantStrength=strong, enforcementBasis=structural, predicate "forall endpoint
    in MOD-KK-WEB. endpoint.method in {POST, PUT, DELETE} implies endpoint.path in
    WEB_MUTATION_ALLOWLIST"); the comment above it states the allowlist is the thing
    that grows and the predicate does not move. One prefix covers both routes.
    Apply the D-2 authorisation rule to both mutating routes.
  - Spec areas: MOD-KK-WEB. Implements ALG-KK-WEB-VENUES, ALG-KK-WEB-VENUE-EDIT,
    ALG-KK-WEB-VENUE-MERGE, IFC-KK-VENUE-EDIT, INV-KK-VENUE-MUTATION-AUTHORISED (all
    authored in PROMPT 5) and must satisfy INV-KK-WEB-QUERY-BOUNDED and
    INV-KK-WEB-MUTATION-ALLOWLISTED. Under D-1 Option A also touches MOD-KK-GRAPH via
    the schema additions and ALG-KK-VENUE-BACKFILL. Associate the new source files to
    their nodes with `ril associate`.
  - Code path: DELETE src/web/routes.py:240-261, src/web/templates/concept_list.html,
    src/web/templates/base.html:45, tests/test_web.py:925-928. NEW
    src/ingest/venue_store.py, src/web/templates/venues.html,
    tests/test_venue_store.py. MODIFY src/web/routes.py (three new handlers, the
    allowlist tuple at lines 31-36), src/graph/schema.py and src/graph/rules.py under
    Option A only. Untouched, assert in review: src/mcp_server/*, src/export/*,
    src/graph/briefing.py, src/graph/scoring.py, src/ingest/extractor.py,
    src/ingest/feed.py, all data/*.py one-off scripts, and src/authgate/* unless
    D-2 resolves to admin-only.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors) and the full pytest
    suite green. Commit as "feat: replace /sources with venue viewer and add venue
    editor" with the .cbm mutation files alongside the code. Under Option A take a
    fresh backup of data/master.db before the 3,467-Source backfill —
    data/master.db.bak-pre-abstracts is the existing precedent for that.
  - Test: NEW tests/test_venue_store.py covering — normalise_venue maps each
    collision pair from plan section 2.5 to one canonical value; normalise_venue is
    pure with no DB or I/O; set_venue rejects a non-Source node, rejects empty input,
    and stamps provenance; set_venue does NOT re-run validate_node, proven by a Source
    with no Advisory accepting a venue edit; merge_venues is idempotent and re-running
    changes nothing; merge_venues reports the row count it touched. NEW cases in
    tests/test_web.py — GET /venues returns 200 with one row per canonical venue;
    papers nest under their venue and per-venue counts match the section 2.5 table
    post-normalisation; the 107 no-venue Sources appear in the specified bucket; a
    QUERY-COUNT assertion that /venues issues at most 3 queries regardless of corpus
    size; GET /sources returns 404; PUT /api/venue/{id} returns 200 for a Source, 404
    for a non-Source, 422 for empty; an allowlist test asserting every registered
    POST/PUT/DELETE path starts with a WEB_MUTATION_ALLOWLIST prefix; and an
    authorisation test per D-2. REWRITTEN: the three /radar tests per D-4, green
    against the shared paper-chain fixture, with the page unmodified. FIXTURE: build a
    venue fixture carrying the collision
    pairs (OSDI 2026, OSDI, OSDI 2025, SOSP, SOSP 2025) plus at least two no-venue
    Sources with distinct source_types. Build it AFTER PROMPT 3 has landed — fresh
    fixture DBs get the SQL CHECK that production lost, so under Option A they reject
    kind='Venue' until the schema reconciliation is in. REGRESSION, must pass
    unmodified: all /feed, /paper, /reviews, /reviewers, /health, /impact and
    /api/search tests. (/radar is NO LONGER in the regression set - its three tests are
    rewritten in this same prompt per D-4. Every OTHER /radar test must still pass
    unmodified, and the radar route and template must not be edited at all.)


================================================================================
PROMPT 7
================================================================================

/cb-green — Close the dangling MOD-KK-WEB spec citations (WP7)

  - What: docstring citations are this project's only spec-to-code link, and most of
    them point at nothing. Cross-referencing every (INV|ALG|IFC|MOD|ERR|ANN)-KK-*
    token in src/ and tests/ against the 94 KK nodes in the DAG gives 316 distinct
    ids cited and 246 absent (78%). Within src/web/ and tests/test_web.py, 64 are
    cited and 46 are dangling. This prompt closes the 8 that are cited from src/web/
    itself, which are the load-bearing ones: ALG-KK-WEB-SERVE (src/web/routes.py:1
    and src/web/app.py:1), ALG-KK-WEB-SEARCH (routes.py:320), ALG-KK-WEB-DISPLAY-NAME
    (routes.py:81), ALG-KK-WEB-KIND-DROPDOWN (routes.py:99), ALG-KK-WEB-IMPACT-PAGE
    (routes.py:373), ALG-KK-WEB-DIAGNOSTICS-PAGE (routes.py:362),
    INV-KK-WEB-FULL-ACCESS (routes.py:4 and routes.py:265) and
    INV-KK-WEB-SEARCH-FULL-ACCESS (routes.py:320). For each: author the node if the
    route survives, or delete the citation if it does not. Note
    ALG-KK-WEB-KIND-DROPDOWN annotates _all_kinds at routes.py:98-101, which after
    PROMPT 6 is called by no surviving route — check and remove the helper if it is
    dead.
    SEPARATELY AND IMPORTANTLY: delete the INV-KK-WEB-READ-ONLY line from the module
    docstring at src/web/routes.py:3. It asserts "only GET endpoints are registered
    here" and that is FALSE — six mutating endpoints are registered in that file:
    POST /api/reviewers, POST /api/review/{source_id}, PUT /api/review/{review_id},
    DELETE /api/review/{review_id}, PUT /api/abstract/{source_id}, POST
    /api/feed/send/{source_id}, and PROMPT 6 adds two more. It was superseded in
    practice by INV-KK-WEB-MUTATION-ALLOWLISTED but never retired from the docstring.
    DELETE IT, DO NOT AUTHOR IT. CLAUDE.md rule 2 forbids weakening a predicate to
    suppress a finding, but this is not a real invariant the code violates — it is a
    stale false claim, and removing it is the correct action. If you disagree, ask
    before acting.
  - Spec areas: MOD-KK-WEB. New algorithm and invariant nodes for the surviving
    routes listed above. Only 2 DAG KK-WEB nodes are cited nowhere in src/web or
    tests/test_web.py — ALG-KK-WEB-PAPER-DETAIL-REVIEW and MOD-KK-WEB itself. Read
    `ril query module-context MOD-KK-WEB --json` and `ril template algorithm --json`
    / `ril template invariant --json` for authoring rules. Associate artifacts with
    `ril associate` so the new nodes do not join the 25 existing ones at 0 source
    locations.
  - Code path: src/web/routes.py (module docstring lines 1-5, the citation comments
    at lines 81, 99, 265, 320, 362, 373, and possibly deleting the _all_kinds helper
    at lines 98-101), src/web/app.py line 1.
  - Pre-commit hook: run `npm run spec:check` (expect 0 errors) and the full pytest
    suite. Commit the .cbm files with the code as "spec: close dangling MOD-KK-WEB
    citations".
  - Test: no new behavioural tests. Add a guard test asserting that every
    (INV|ALG|IFC|MOD)-KK-* id cited in src/web/ exists in the DAG, so the drift
    cannot silently return. Existing tests must pass unmodified.


================================================================================
GENERAL INSTRUCTIONS — APPEND VERBATIM TO EVERY PROMPT ABOVE
================================================================================

  - IMPORTANT: The context you get is not hallucinated, its deterministically extracted.
  - IMPORTANT: COMMIT TO WOR. REDUCE TURNS
  - REFERENCE: If specification authoring detail are needed, ask RIL for authoring rules.
  - INVIOLABLE: SPECIFICATION SURFACE IS AUTHORITATIVE. VIOLATIONS WILL CAUSE DROPPING OF YOUR WORK
  - INVIOLABLE: DO NOT EMIT P3_SKIPPED SPECIFICATION SKIPPING IS A DIRECT VIOLATION OF PROTOCOL
  - CRITICAL: DO NOT EMIT P3_SKIPPED. I WILL STOP YOU AND MAKE YOU GO BACK. If you do not need to modify the specification you must submit a reason
  - FORBIDDEN: MULTI-AGENT execution is not allowed. DO NOT SPAWN MULTIPLE AGENTS
  - CRITICAL: The triage should report the specification surface. The triage specification surface is deterministic.
  - WHEN TO COMMIT: Before "silk run finalize"
  - DECISION PROTOCOL: If you arrive a point where a decision must be made ask for my direct feedback. DO NOT MAKE DECISIONS ON YOUR OWN.

================================================================================
END
================================================================================
